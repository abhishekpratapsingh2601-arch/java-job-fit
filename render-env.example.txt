SPRING_PROFILES_ACTIVE=prod
SPRING_FLYWAY_ENABLED=true
SPRING_FLYWAY_BASELINE_ON_MIGRATE=false
SPRING_JPA_HIBERNATE_DDL_AUTO=validate
PAYMENT_PROVIDER_ENABLED=false
SPRING_DATASOURCE_URL=jdbc:postgresql://db.YOUR_PROJECT_REF.supabase.co:5432/postgres?sslmode=require
SPRING_DATASOURCE_USERNAME=your_supabase_db_username
SPRING_DATASOURCE_PASSWORD=your_supabase_db_password
ALLOWED_ORIGINS=https://abhishekpratapsingh2601-arch.github.io

Optional (per-IP rate limits, defaults shown):
RATE_LIMIT_API_POST_PER_MINUTE=30
RATE_LIMIT_EXTRACT_PER_MINUTE=10

Important note:
Use the Supabase direct PostgreSQL connection on port 5432 for Flyway migrations.
Do not use the transaction pooler port 6543 for Flyway.
Do not set SPRING_FLYWAY_BASELINE_ON_MIGRATE=true for this first deploy because the JavaJobFit app schema is empty.
application-prod.yml already pins:
DATABASE_DRIVER=org.postgresql.Driver
HIBERNATE_DIALECT=org.hibernate.dialect.PostgreSQLDialect

# Payments (Session B). Leave PAYMENT_PROVIDER_ENABLED=false until the Razorpay account is live:
# while false the payment endpoints return 503 and no Razorpay call is ever made.
PAYMENT_PROVIDER_ENABLED=false
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
# From the Razorpay dashboard webhook you create for payment_link.paid. Without it, every
# webhook is rejected as unsigned, so nothing can ever be unlocked.
RAZORPAY_WEBHOOK_SECRET=
# Launch price and struck-through anchor (see PAYMENT_TODO.md).
PRO_PRICE_INR=89
PRO_ANCHOR_PRICE_INR=299
# Where Razorpay returns the buyer; the report id is appended automatically.
PRO_RETURN_URL=https://abhishekpratapsingh2601-arch.github.io/java-job-fit/
